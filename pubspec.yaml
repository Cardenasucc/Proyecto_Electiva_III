name: proyecto_electiva_iio
description: "Proyecto para simular una app financiera "

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  syncfusion_flutter_charts: ^22.2.5

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

